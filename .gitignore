# Dependencies
node_modules/
package-lock.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Large binary files
# (keep scripts and source code, but not builds)

# Temporary files
*.tmp
*.swp
*~

# OpenClaw internal (not part of workspace backup)
.openclaw/
.clawhub/

# Environment files (may contain API keys)
.env
.env.*
*.env
secrets/
credentials/

# Secrets storage (local only, never commit)
.secrets/

# Cron backups (contain system-specific data)
cron-backup/

# OpenClaw config (contains real API keys and tokens)
# Use openclaw.template.json as template
openclaw.json
*.backup.*

# Backup files
*.bak
*.backup
*.db
data/
